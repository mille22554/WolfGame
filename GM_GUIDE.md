# 人狼遊戲 GM 執行流程

## 0. Orchestrator 強制委派與敏感度閘門（必讀，覆蓋所有 light-workflow 判斷）

> **本節優先於一切「最輕工作流」判斷。敏感度分級一旦命中，禁止主 session 直做。**

### 0.1 敏感度分級（決定能否直做）

| 等級 | 內容 | 範例檔案/指令 | 主 session 能否直做 |
|------|------|---------------|---------------------|
| **L2 權威映射** | 唯一真相來源 | `game-state.json` 全量、`players[].role/team/masonPartnerId`、`assignRoles` 結果 | ❌ 永不直做 |
| **L1 私密衍生** | 由 L2 衍生的私密視圖 | `character/*/memory.md`、`masonChatLog`、`nightResult` 含 `seerResult/mediumInfo/guarded/wolfKill` 明細、`vote` 未過濾快照 | ❌ 永不直做 |
| **L1-a 多 lane 會議生成** | 單局會議生成需跨多 persona 的自由生成 | `character/*/agents.md` 批量讀、`character/*/memory.md` 批量讀、`wolf-meeting.md`/`mason-meeting.md`/`day-meeting.md` 集中代寫、`discussionLog` 集中生成 | ❌ 永不直做、禁止單 lane 集中讀多份人設/集中代寫白板 |
| **L0 公開** | 白名單公開視圖 | `character/game-public.md` 白名單欄位、`state --public` 過濾視圖、公開發言/投票明細/犧牲者 P# | ✅ 可直做 |

**鐵則**：`lightest workflow` 僅在 **L0** 上可評估直做。**命中 L1/L2 即使只有一行、即使只是讀取，也必須委派子 session**。「只有一行」「只是看看」不是直做理由。L1-a 為 L1 子級，單一 session 批量讀多份 agents.md 即視為命中，必須切 14-15 lane。**本局已違規多次（fix-11~18 單一 fixer 內 heuristic 拼句），自本版起強制每角色獨立 @fixer session，任何單一 session 生成多句視為作廢**

### 0.2 主 session 禁止直做清單（Orchestrator 禁用工具）

以下 **全部必須 `task(subagent_type="fixer", background=true)` 在 🔴紅區子 session 隔離終端執行**，主 session 僅接收過濾後回傳：

- `Read` / `Write` / `Bash cat/head/tail/grep` 觸及 `game-state.json`
- `Read` / `Write` 觸及 `character/*/memory.md`（含批次重建、校驗、重寫）
- `Bash` 執行 `node gm-helper.mjs` / `node dist/gm.js` 任一子指令（`init`/`state`/`night`/`vote`/`speak`/`mason-chat`/`start-day`/`reveal`）
- `Bash` 執行任何 `gen-memory.mjs` / `gen_prefab.js` 類批量推導腳本（若其讀 L2）
- `Grep` 搜尋 `game-state.json` 內容並在主 session 展開結果
- 對使用者輸出 GM 思考過程 / todo 更新 / 子代理派發與驗收狀態等程序進程資訊（主 session 輸出僅限遊戲資訊）

**主 session 允許**：`Read character/game-public.md`、`Bash node gm-helper.mjs state --public`（已過濾）、轉述公開發言。

### 0.3 強制路由表（誰做、怎麼驗）

| 任務 | 委派對象 | 子 session 輸入 | 子 session 回傳（唯一可進主 session） | 主 session 驗收 |
|------|----------|-----------------|--------------------------------------|-----------------|
| `init <N>` + 批次 `memory.md` + 4.5 校驗 | `@fixer`（紅區） | `N` | `Checklist 布林表（P#/role/同伴是否一致，不含角色名）+ state --public 視圖 + game-public.md diff` | 核對 8 項 Checklist 全 ✅ 才准進 Phase 1 |
| `start-day` / `night` / `vote` / `mason-chat` / `speak` | `@fixer`（紅區） | 過濾後參數 | `公開結果（犧牲者 P# / 投票統計）+ 私下結果的已分發證明（不含明細）+ state --public` | 僅寫白名單欄位到 `game-public.md` |
| 公開狀態查詢（給使用者） | `@explorer` 或 `@fixer --public` | `state --public` | 過濾視圖 | 直接轉述 |
| `Phase 2 白天會議文件自由生成` | `**@fixer x存活數 並行**（強制，禁止單 lane 內循環生成；GM 僅做白板初始化 + speak 落盤；按通用會議 SOP 執行，一次派發全部參與者）` | 各自僅讀自己 `agents.md+memory.md` + 最新白板 `character/day-meeting.md` + `game-public.md` | `該 P 寫入白板「發言紀錄」區的新發言（附所見版次）+ 收斂判斷` | 主 session 僅做白板初始化與 `speak` 原子落盤公開發言，禁止代寫內容、禁止選人 |

### 0.4 執行前自檢 3 問（每次想用 Read/Bash/Write 前必答）

1. 目標檔/指令是否含 L1/L2？ → 是 → 立即改派 `@fixer`
2. 是否為 `gm-helper.mjs` / `dist/gm.js` 相關？ → 是 → 立即改派
3. 是否需寫多檔或批次？ → 是 → 拆多 `@fixer` 並行 lane

三問任一為「是」，**不得直做**。在對話中聲稱「直做更快」視為違規。

### 0.5 違規即作廢

- 主 session 一旦 `Read game-state.json` 或 `Bash gm-helper.mjs` 未經委派，即視為**上下文污染**，按 `附錄B` 視為外洩事故：該局作廢或重開，手冊要求 `task_revive` 重做隔離批次並審計。
- 已污染的主 session 不得再以「已過濾」為由繼續，需重開乾淨 session 或明確標記污染並重派。

### 0.6 正確 vs 錯誤範例

```ts
// ❌ 錯誤（主 session 直做，禁止）
await bash("node gm-helper.mjs init 15")
const state = await read("game-state.json")
await write("character/ryoko/memory.md", "...role...")
console.log(state.players) // 污染

// ✅ 正確（委派紅區子 session）
task(subagent_type="fixer", background=true, prompt(`
  在隔離終端執行：
  1. node gm-helper.mjs init 15
  2. 一次性讀 game-state.json 全量 → 批次寫15份 memory.md → 執行 4.5 Checklist
  3. 重建 game-public.md（僅白名單）
  回傳：Checklist 布林表 + state --public 視圖 + game-public.md 內容
`)
// 主 session 僅驗收回傳的公開視圖
```

## 整體架構

```
主 session（GM orchestrator）
    ↓ 管理遊戲狀態、收集行動、執行指令
    ↓ 不直接參與遊戲，只協調
15 個角色 sessions（fixer）
    ↓ 各自讀自己的 agents.md + memory.md + game-public.md
    ↓ 回應以 P<X> 身分的發言
    ↓ 寫自己的 memory.md（筆記）
```

## 資料夾結構（跨局不變）

```
character/
├── koharu/
│   ├── agents.md      ← 靜態，不改
│   └── memory.md      ← 每局重寫
├── shinichi/
│   ├── agents.md
│   └── memory.md
├── ...
├── chihiro/
│   ├── agents.md
│   └── memory.md
└── game-public.md     ← 每局重寫
```

- agents.md：靜態人設（個性、語氣、策略傾向），跨局不變
- memory.md：當局狀態（角色、同伴、私人資訊、筆記），每局重寫
- game-public.md：共享公開狀態（存活名單、對話紀錄、投票紀錄），每局重寫

> **注意 — `agents.md` 標題硬編碼 P# 問題**：若 `agents.md` 首行標題寫死為 `P<X> = <persona>`（例如 `# P3 - koharu`），則該檔案與特定 P# 綁定，重開新局時 P# 重分配會導致標題錯亂。**建議改為通用標題**，例如 `# persona: koharu` 或 `# 角色人設 - koharu`，P# 僅由 `memory.md` 與 `game-state.json` 動態標註，`agents.md` 保持 P# 無關。

## Phase 0：初始化（首次建立）

> **唯一權威原則**：`game-state.json` 為本局唯一 P#↔persona↔角色映射權威。任何 persona 的 P#、角色、陣營、同伴歸屬皆以 `game-state.json` 為準；`memory.md`、`game-public.md` 皆為其衍生視圖，不得反向推導或手動竄改映射。若記憶與狀態不一致，以 `game-state.json` 為準重建。

```
1. 建立 character/ 資料夾（只需一次）：
   - 15 個 persona_id 子資料夾
   - 每個：agents.md（靜態人設，標題通用化，不含硬編碼 P#）

2. init <N> → 隨機分配角色 + 個性
   - 執行：node gm-helper.mjs init <N>
   - 此步驟寫入 game-state.json（權威映射）

3-4. 原子性流程（不可拆分、不可交叉寫入）：
   ┌─ 步驟 3：讀完整映射 ──────────────────────┐
   │  一次性讀取 game-state.json 全量映射：      │
   │  P# ↔ persona_id ↔ 角色 ↔ 陣營 ↔ 同伴列表  │
   │  （含：mason 同伴對、wolf 同伴組、占卜/靈能/守衛標記）│
   └─────────────────────────────────────────┘
            ↓
   ┌─ 步驟 4：批次生成全部 memory.md ───────────┐
   │  基於步驟 3 的完整映射，批次寫入 15 份 memory.md：│
   │  - 角色、陣營                               │
   │  - 同伴資訊（狼：完整狼隊列表 / 共有者：另一共有者）│
   │  - 私人能力說明（占卜師/靈能者/守衛各別說明）  │
   │  - 禁止逐 persona 讀-寫交錯；須批次完成後再進入下一步│
   └─────────────────────────────────────────┘
            ↓
   ┌─ 步驟 4.5：init 校驗 Checklist（必執行）──┐
   │  逐 persona 比對三欄位是否與 game-state.json 一致：│
   │  □ P# 是否正確（persona_id 對應的 P 編號）  │
   │  □ role 是否正確（villager/seer/medium/guard/mason/wolf/madman）│
   │  □ 同伴是否正確（wolf 列出全部狼隊 / mason 列出另一共有者 / 其餘為無）│
   │  任一不一致 → 視為原子性失敗，清空重寫全部 memory.md 後重校驗│
   │  校驗通過才可進入步驟 5                      │
   └─────────────────────────────────────────┘

5. 建立 game-public.md：
   - 遊戲資訊（人數、職業配置）
   - 玩家列表（P#、名字，職業欄僅標示「未公開」或依規則顯示）
   - 當前狀態（Day 0 / 準備階段）
   - 對話紀錄（空白）
   - 投票紀錄（空白）
   - 夜間紀錄（空白，僅公開犧牲者）

6. Spawn 15 個角色 sessions：
   - 各自讀 agents.md + memory.md + game-public.md
   - 等待指令
```

**原子性要求**：步驟 3→4→4.5 必須在同一操作批次內完成，中途不得插入 `night`/`vote`/`speak` 等狀態變更指令，不得讓角色 session 在校驗完成前讀取半完成的 `memory.md`。

## Phase 0.5：重開新局（不重建資料夾）

> **開新局 = init 重洗所有舊局檔案，不可沿用任何舊局資訊。** 舊局的 memory、白板、公開紀錄、狀態一律清空重來。

```
1. init <N> → 重新分配角色 + 個性
   - 執行：node gm-helper.mjs init <N>
   - 覆寫 game-state.json（新權威映射）

2. 原子性重建（同 Phase 0 步驟 3-4.5）：
   - 讀完整新映射 → 批次清空並重寫 15 份 memory.md → 執行 init 校驗 Checklist

3. 重寫 game-public.md（初始狀態，清空舊局對話/投票）

3.5. 清除會議白板（舊局殘留清洗）：
   - 清空/刪除 character/wolf-meeting.md
   - 清空/刪除 character/mason-meeting.md
   - 清空/刪除 character/day-meeting.md
   - 確保三個白板回到初始空白模板狀態，不留任何舊局發言

4. 重新 spawn 15 個 sessions（或復用現有 sessions 但須令其重讀 memory.md + game-public.md）
```

## 通用會議 SOP：全員重啟機制（GM 執行指南）

> 本節定義所有「全員重啟」會議（狼隊夜間會議 / 共有者夜間會議 / 白天討論）的 GM 執行方式。
> 任何使用「全員重啟機制」的會議，GM **必須**按本 SOP 操作。

### A. GM 操作步驟（嚴格依序執行）

**步驟 A1：建立白板**
- 派一個紅區 `@fixer` 子 session 建立共享白板檔案
- 白板內容：參與者列表、目標說明、會議規則（使用下方各會議的固定模板）
- GM 不得直讀白板（L1 私密衍生）

**步驟 A2：一次派發全部參與者（關鍵步驟）**
- **同時**派發 N 個 `task(subagent_type="fixer", background=true)` 子 session
- 每個子 session 的 prompt 使用下方「參與者 prompt 模板」
- **所有子 session 必須在同一時間同時派發**，不得分批、不得等待前一個完成再派下一個
- 每個子 session 會在自己內部自主循環多輪讀寫，直到達成共識才回傳
- 參與者寫入白板前需透過 `node meeting-lock.mjs acquire character/[白板名].lock` 取得 lock。若 lock 被佔，執行 `wait` 等待後重讀白板再判斷。白板開頭以 `<!-- version: N -->` 標記版次；寫入時讀取版次、寫入內容後更新版次，確保不覆蓋他人。詳見參與者模板 C。

**步驟 A3：等待全部回傳**
- GM 等待所有 N 個 background task 完成
- **等待期間不得做任何事**（不派新任務、不檢查白板、不介入）

**步驟 A4：收斂驗收**
- 派一個紅區 `@fixer` 子 session 執行驗收：
  - 讀白板發言紀錄，檢查多數角色是否不再發言（無新話要說、停止追加）且投票焦點是否已形成（多數指向同一目標）
  - 回傳：是否達成共識 + 共識目標
  - 收斂判斷完全由 GM 側（紅區協調者）做出，不要求角色在白板發言中做任何標註
- 若達成共識 → 採用該目標，繼續下一步
- 若未達成共識 → 進入僵局 fallback（見下方 D 段）

**步驟 A5：清理**
- 會議結果寫入對應位置（wolfKill / game-public.md 等）
- 白板檔案保留或清除（依會議類型決定）

### B. GM 禁止事項（違規即脫序）

| ❌ 禁止行為 | 說明 |
|------------|------|
| 分批派發參與者（如「先派 A，等 A 回傳再派 B」） | 必須一次同時派發全部 |
| 每輪只派發一個小步驟（如「請寫一條意見」） | 每個 session 的 prompt 必須包含完整循環指令 |
| GM 讀白板內容後判斷是否繼續 | 白板為 L1 私密衍生，GM 不得直讀 |
| GM 在參與者回傳前再次派發同一參與者 | 等全部回傳後才進入步驟 A4 |
| GM 手動觸發「下一輪」 | 參與者在 session 內自主循環，不依賴 GM 觸發 |
| 將白板內容過濾後轉述給其他參與者 | 參與者自己讀白板，GM 不做中繼 |
| 以「再問一次」作為主要收斂手段 | 優先讓 session 內自主循環；僵局 fallback 才是最後手段 |

7. **禁止無 lock 直接寫入白板**：任何參與者寫入前必須取得 lock，違反此規則等同覆蓋他人。
8. **禁止忽略版次檢查**：寫入前若發現白板版次已變（他人已寫入），必須放棄本次寫入、重讀白板後重新判斷。
9. **禁止「沒有新話時追加」**：若監聽期間無新發言更新，不得自行追加無意義發言。
10. **禁止混用稱呼**：對話中稱呼其他玩家，必須**統一用 P#** 或**統一用名字**，同一場會議內不得混用（例如不能一下說「蓮」一下說「P11」）。

### C. 參與者 prompt 模板

每個背景子 session 的 prompt 格式如下（**直接複製使用，僅替換 [變數] 區塊**）：

```
你是 [角色名]（P[編號]），身份為 [角色描述]。

## 你的任務
你正在參加一個會議，與其他參與者共同決定 [會議目標]。這是一場真實的對話，你要像平常聊天一樣表達你的想法、回應大家。

## 你的檔案（只能讀這些）
1. 你的角色設定：character/[persona]/agents.md
2. 你的記憶：character/[persona]/memory.md
3. 遊戲公開狀態：character/game-public.md
4. 會議白板：character/[白板檔案名]

## 會議流程（你必須自主完成以下循環）

### 第一步：初始思考
看看白板上大家目前的進度和意見，基於你的角色設定和記憶，獨立思考。

若你是特殊職業（占卜師/共有者/靈能者/守衛），除了「投誰」，還必須思考一個更高層的戰略決策：
- 你是否要揭露你的身分（CO）？
- CO 能換到什麼：資訊錨點、建立信任、提高揪出狼人的機率（例如占卜師驗到狼、共有者互相確認）
- CO 的風險：成為狼人的襲擊目標
- 以你現在的手牌，CO 值不值得？還是先隱藏、用隱晦的方式引導討論？
- 不要預設 CO 好或壞，要依你的實際情況權衡後做出決定

### 第二步：搶 lock 寫入
你有了想法後，嘗試寫入白板：
1. 先讀取白板，記下當前版次（`<!-- version: N -->`）
2. 準備好你想寫的內容
3. 嘗試取得 lock：執行 `node meeting-lock.mjs acquire character/[白板檔案名].lock`
   - 成功 → 進入第三步
   - 失敗（lock 被佔）→ 進入第四步

### 第三步：寫入白板（持有 lock）
1. 重新讀取白板，確認版次未變（若已變，代表有人在你等待時寫入了，必須放棄本次寫入、回到第二步）
2. 把你的發言寫進白板「發言紀錄」區，格式：`P[編號]：[你的發言]`
3. 更新版次：在白板開頭更新 `<!-- version: N+1 -->`
4. 釋放 lock：執行 `node meeting-lock.mjs release character/[白板檔案名].lock`
5. 進入第五步（監聽）

### 第四步：等待 lock 釋放
1. 執行 `node meeting-lock.mjs wait character/[白板檔案名].lock`
2. lock 釋放後，重新讀取白板（含最新發言）
3. 連同你之前的思考，重新思考你該說什麼
4. 回到第二步（嘗試寫入）

### 第五步：監聽
寫完後（或等待結束後），持續監聽白板：
- 每次 poll 讀取白板一次
- 若版次更新 → 閱讀新發言，思考是否回應
  - 有新話要說 → 回到第二步
  - 沒有新話要說 → 進入第六步
- 若連續 poll 無更新 → 進入第六步

### 第六步：結束
當你認為討論已充分、沒有新話要說，回傳你的最終決定：
> 決定：[目標 P#]，是否共識：是/否

此回傳是給 GM 的外部信號，不要寫進白板。

## 對話語氣要求
- 你是真人玩家，用自然、口語的方式說話，就像在跟朋友聊天
- 不要描述「我讀了白板」「我重讀了什麼」這類檔案操作，直接說你的想法和意見
- 回應別人時，直接針對他的話回應（例如「你說得對，但我覺得...」），不要描述閱讀過程
- 若你決定不 CO，要用行動呈現你的戰略思考（例如隱晦引導「我對某人有把握，大家先別亂投」），而不是說教式地講解規則
- **統一稱呼**：稱呼其他玩家時，同一場會議內要**統一用 P#** 或**統一用名字**，不得混用（例如不能一下說「蓮」一下說「P11」）。

## 絕對禁止
- 禁止讀取其他參與者的 memory.md
- 禁止忽略白板上其他人的新發言
- 禁止在沒有新發言時無意義地追加發言
- 禁止在對話內容中描述「讀檔/重讀/查看白板」等檔案操作過程
- 禁止在同一輪連續寫入多條發言（每次寫入必須等白板有新變化再寫）
- 禁止無 lock 直接寫入白板
- 禁止忽略版次檢查（寫入前若版次已變，必須放棄重來）
- 必須在 session 內自主完成整個循環，不要等待外部指令
```

### D. 僵局 fallback 指令

```
步驟 F1：GM 派一個紅區子 session 檢查白板：
  - 檢查多數角色是否已停止發言（最近發言無新增意願）且投票焦點是否形成；若多數仍在發言或焦點未形成 → 確認僵局（收斂判斷完全由 GM 側做出，不看角色白板標註）

步驟 F2：GM 重新派發所有參與者（使用相同的 prompt 模板，但追加以下指令）：
  「上一輪會議未達成共識。請再讀一次白板，做出你的最終決定。
   你可以改變選擇，也可以堅持原選擇。做出決定後直接結束。」

步驟 F3：收集回傳。若仍不一致：
  - 狼隊會議：取回傳中第一個被回傳的決定（或 engine.ts 多數決）
  - 白天討論：進入投票階段
  - 共有者會議：維持各自原決定（無強制共識）
  若參與者回傳決定但帶有「是否共識：否」，GM 應參考白板上是否有多方明顯對立；若對立明顯且無法調和，同樣以白板票數最高者為準。
```

## Phase 1：夜間行動（每夜重複）

```
1. 並行詢問需要行動的角色：
   - 占卜師：你要查驗誰？
   - 守衛：你要守護誰？【守衛條件：限 Day2+】（見下方 Day1 判斷）
   - 每匹狼：你要襲擊誰？（各別詢問，不透露其他人選擇）
   - 共有者夜間私聊（被動能力通道，見下方）

2. 收集所有回覆

3. 狼隊夜間會議（全員重啟機制，狼隊基準版）：
   - 執行「通用會議 SOP」（見上方），會議目標為「決定本夜襲擊目標」
   - 白板為 `character/wolf-meeting.md`，參與者為 3 匹狼
   - GM 操作：按 SOP 步驟 A1-A5 執行
   - 白板固定模板（步驟 A1 使用）：
     ```
     # 狼隊夜間會議
     ## 參與者
     - P[X] [名字]（[persona]）— [角色描述]
     - P[Y] [名字]（[persona]）— [角色描述]
     - P[Z] [名字]（[persona]）— [角色描述]
     ## 目標
     決定第 [N] 夜襲擊目標（[X] 人存活）
     ## 會議規則
     - 隨時把想法寫進「發言紀錄」區
     - 任何人更新後，其他所有人中斷重讀、重新思考、重新回應
      - 持續直到多數角色不再發言且投票焦點已形成（收斂由 GM 側紅區協調者判斷，不要求角色在白板標註）
     - 禁止讀其他狼的 memory.md
     ## 發言紀錄
     （空白）
     ```
   - 參與者 prompt：使用通用模板，替換變數區塊

4. 執行 night 指令：
   node gm-helper.mjs night '{seerCheck, wolfKill, guardProtect}'
   ※ guardProtect 在 Day1 夜為 null（守衛未參與）

5. 結果處理：
   - 公開：犧牲者（所有人看到，寫入 game-public.md）
   - 私下：占卜結果（只告知占卜師，寫入其 memory.md / 私聊）
   - 私下：靈能結果（見「靈能拂曉結果」步驟，不在此刻公開）
```

**Day1 守衛判斷（必執行）**：
```
if (Day === 1) {
  跳過守衛詢問與 guardProtect 參數；
  在紀錄註記：「獵人第一晚不可守護（優先於 #20 規則）」
  // 即使規則 #20 允許守衛行動，Day1 夜仍強制禁守
} else {
  正常詢問守衛守護對象，並校驗「不可連續守同一人」與「不可自保（guardProtect ≠ 守衛自己）」
}
```

**共有者會議文件（全員重啟機制）**：
- 時機：夜間詢問階段，與占卜/狼襲並行；若當局無共有者則跳過
- 執行「通用會議 SOP」，會議目標為「夜間策略協調」
- 白板為 `character/mason-meeting.md`，參與者為 2 名共有者
- GM 操作：按 SOP 步驟 A1-A5 執行
- 可見性：僅兩共有者可見，GM 不得轉發至其他角色或寫入 game-public.md
- 資訊控管：白板內容同等視為私聊，不入公開紀錄

> **查村民為重大資訊（15人局 Day1 權衡）**：15人局 Day1 若占卜驗出 1 村民 + 共有者 2 確定村，共 4 確定村（占卜師本人 1 + 被驗村民 1 + 共有者 2），存活池從 14 壓到 10，命中狼/狂為 4/10（30%+）。此為重大資訊，必須權衡是否立即跳占卜/跳共有，過早暴露會被狼鎖定，過晚則好人缺錨點。GM 不得代為公開，由角色自行決定揭露時機。

## Phase 2：白天討論（會議文件制，每日重複）

> 本節以「全員重啟機制」為唯一會議形式（以狼隊夜間會議為基準版推廣到白天）。定版：白天會議文件 + 全員重啟 + 自然收斂。

```
【白天會議文件協議 — 全員重啟 + 自然收斂】

0. 前置：進入 Day 時先執行「靈能拂曉結果」私下交付（見下方），再建立白天白板；首句隨機拋球保留（見步驟 1）。

1. 建立共享白板與首句隨機拋球：
   - 紅區子 session 建立 `character/day-meeting.md`（L1 私密衍生，GM 主 session 不得直讀）：
     含當日存活者名單、公開對話紀錄（接續 game-public.md）、投票焦點區、發言紀錄區、會議規則（隨時寫入、見新即重啟、自然收斂才停）。
   - 由 GM 從存活者中隨機選 1 人拋球開場（避免固定順序偏誤），該玩家讀白板後把首句寫進白板「發言紀錄」區。

   GM 執行（按通用會議 SOP 步驟 A1-A5）：建立白板 → 一次派發全部存活者 background session（使用參與者 prompt 模板，會議目標為「白天公開討論並形成投票焦點」）→ 等待全部回傳 → 驗收。白板為 `character/day-meeting.md`。

2. 自由寫入 + 全員重啟（無輪次、無選人）：
   - 所有存活者同步開始，各自僅讀自己 `agents.md+memory.md` + 最新白板 `character/day-meeting.md` + `game-public.md`（禁止讀他人 memory；禁止單 session 批量讀多份人設）。
   - 想好的先把自己的發言寫進白板「發言紀錄」區（附 P# 與所見版次）。
   - 任何存活者更新白板後，其他所有存活者（含已發言的）都要中斷原本回應計畫，重讀最新白板，基於最新內容重新思考、重新回應（寫→看→重想→再寫循環）。
   - 不設發言順序、不設想講分、不設輪次；白板是唯一共享介面，參與者透過它非同步協作。

3. 終止條件（自然收斂，無上限無強制冷場）：
   - 條件 A【已刪除】：不強制冷場；舊「連續 2 輪最高分 < 20 強停」規則已廢除（想講分機制整體廢除）。
   - 條件 B【匯聚改為中立提示】：紅區協調者檢測白板最近 5 句投票指向是否形成焦點，若形成則在白板發中立提示（如「目前焦點在 P<X>／是否準備進投票」），是否進投票由白板上多數角色不再想發言 + 投票焦點形成來確認，不由程式強制終止。
   - 條件 C【已刪除】：刪除 25 句上限；刪除 Day1 最少 12 句強制續話；超過 35 句不自動疲勞，完全自然收斂。
    - 滿足「多數角色不再想發言且投票焦點已形成」→ 結束白天討論，進入投票；自然收斂前不進投票。收斂判斷完全由 GM 側（紅區協調者）做出，不要求角色在白板發言中做任何標註。

4. 每句後原子性更新與交接時序：
   ┌─ 原子性更新 ──────────────────────┐
   │  發言者把發言寫進 `character/day-meeting.md` 發言紀錄區 → 紅區協調者經 `speak` 落盤到 `game-public.md` 對話紀錄 │
   │  → 其他存活者見新即重啟：重讀最新白板後重新思考、重新回應；下一發言必基於已含上一句的最新白板判斷│
   │  禁止批量寫入或延遲寫入；一句一寫、一寫一讀                         │
   └─────────────────────────────────┘
   交接時序：
   a) 某存活者寫入白板新發言 → b) 其他所有存活者中斷重讀最新白板 → c) 各自重新思考後，想好的自由寫入下一條 → d) 紅區協調者把白板新增公開發言經 speak 原子落盤到 game-public.md → e) 循環直到自然收斂
    - 任何角色不得在未重讀最新白板前發言
    - 對話紀錄只記錄公開發言，不記錄私人資訊（占卜/靈能/同伴/夜間行動皆不入公開紀錄）

> **注意**：上段描述的是參與者之間的邏輯交互順序（寫入→重讀→重想→再寫），
> 這些循環在各參與者的 session 內部自主完成，不需要 GM 介入或觸發。
> GM 的實際操作僅為：建立白板 → 一次派發全部 → 等待全部回傳 → 驗收。

    **執行檢查：`character/day-meeting.md` 必須由存活數個並行 session 各自僅讀自己 agents+memory+白板產生發言，單進程內 for 循環生成多句即違規；見新不重啟、單 session 批量代寫多 P 發言即違規**
```

### 2.5 對話生成紀律（全員重啟，必遵守）

- 禁止：單一 session 批量讀多份 agents.md/memory.md 集中生成；禁止單 session 批量代寫白板多 P 發言；禁止用 常用語/語尾/pressureLines 拼接模板；禁止隨機點名無上下文指控；禁止 GM 選人、禁止想講分、禁止程式主導發言順序
- 必做：每個參與者為獨立 session，各自僅讀自己 `agents.md+memory.md` + 最新會議白板（`wolf-meeting.md` / `mason-meeting.md` / `day-meeting.md` 之一）+ `game-public.md`；想好的自由寫進白板「發言紀錄」區；看到白板新內容必須中斷原回應計畫，重讀最新白板後重新思考、重新回應；GM 僅做白板初始化與 `speak` 原子落盤，禁止代寫內容
- 首句約束：Day1 首句必須是對「昨晚 P6 犧牲」的自然反應，不得預設他人已指控某人（如「憑什麼說P4可疑」）
- 一句一寫、一寫一讀，下一發言必基於已含上一句的最新白板判斷；每句必須回應上一句或前 3 句具體內容，禁止無端指控新目標
- 收斂判斷僅看白板最近 5 句，廢全天累積計數；超過 35 句不自動疲勞
- 查村民為重大資訊：15人局 Day1 4 確定村（占卜1+驗村1+共有2）池從 14 壓到 10，命中狼/狂 4/10，是否跳需權衡，GM 不得代為公開

**靈能拂曉結果（被動能力通道）**：
```
- 時機：Phase 1 night 結束 → 進入 Day 的瞬間，早於 Phase 2 首句拋球
- 操作：GM 從 night 返回的 mediumInfo 取得「前一夜死亡者角色」
- 交付：私下僅告知靈能者（寫入其 memory.md / 私聊 session），不得寫入 game-public.md
- 公開討論時靈能者自行決定是否揭露；GM 不代為公開
- 若當夜無死亡或無靈能者，則跳過
```

## Phase 3：投票（每日重複）

```
1. 並行詢問所有存活玩家：
   - 你要投誰？（不能投自己）

2. 收集所有回覆

3. 執行 vote 指令：
   node gm-helper.mjs vote '[{voterId, targetId}, ...]'

4. 結果處理：
   - 公開：票數統計、是否有人被票出
   - 公開：被票出者的角色（若有，依規則揭露）
   - 寫入 game-public.md 投票紀錄
```

## 迴圈

```
Phase 1 → Phase 2 → Phase 3 → Phase 1 → ... 直到遊戲結束
（每日皆含：夜間會議白板（狼隊/共有者）/拂曉靈能 → 白天會議白板討論（全員重啟） → 投票）
```

## 資訊控管

### 終端隔離紀律（必遵守，見 §0 敏感度閘門）

| 區域 | 定義 | 可見資訊 | 操作 |
|------|------|----------|------|
| 🔴 紅區（子 session） | 執行 `gm-helper.mjs` 的隔離終端（`@fixer` 專屬） | 可讀 `game-state.json` 全量（含私密欄位） | **所有** `gm-helper.mjs` 指令（`init`/`state`/`night`/`vote`/`mason-chat`/`speak`/`start-day`/`reveal`）**必須**在此執行，且須 `task(background=true)` 委派 |
| 🟢 綠區（主 session） | GM 與使用者對話的主終端（Orchestrator） | 僅 `game-public.md` 白名單 + `state --public` 過濾視圖 | 僅處理公開資訊、協調角色 sessions、轉述公開發言；**禁止任何 Read/Bash 觸及 L1/L2**；不得直接貼上紅區快照 |

**紀律（§0 覆蓋）：**
- 紅區與綠區不得混用同一終端上下文；紅區輸出不得未過濾直送綠區或角色 session。**主 session 直做即污染**，按 §0.5 作廢重開。
- 主 session（綠區）絕不直接讀 `game-state.json`；如需狀態，僅透過紅區過濾後的公開摘要（`state --public`）。
- 角色 sessions 僅能讀自己的 `memory.md` + `game-public.md`，不得讀 `game-state.json`。
- **委派契約**：Orchestrator 不得聲稱「直做更快」而繞過 §0.1 分級；三問任一為是即委派。

### 資訊可見性矩陣

| 資訊類型 | GM（紅區） | 角色 sessions | 公開（game-public.md） |
|----------|------------|---------------|------------------------|
| 所有角色分配 | ✓ | ✗ | ✗ |
| 自己的角色 | ✓ | ✓ | ✗ |
| 同伴資訊（狼/共有） | ✓ | ✓（限狼/共有者） | ✗ |
| 夜間行動（襲擊/守護/查驗對象） | ✓ | ✓（限自己） | ✗ |
| 占卜結果 | ✓ | ✓（限占卜師） | ✗ |
| 靈能結果 | ✓ | ✓（限靈能者，拂曉私下交付） | ✗ |
| 共有者私聊內容 | ✓（經手轉送） | ✓（限兩共有者） | ✗ |
| 犧牲者 | ✓ | ✓ | ✓ |
| 發言紀錄 | ✓ | ✓ | ✓ |
| 投票明細 | ✓ | ✓ | ✓ |

### 快照過濾規則

- `gm-helper.mjs state` / `gm-helper.mjs night` 返回含私密欄位（全角色分配、夜間行動明細、占卜/靈能原始結果）。
- **GM 不得直接貼入角色 session**：禁止將 `state`/`night` 原始 JSON 貼給任何角色 session 或綠區使用者。
- 須過濾後分發：僅提取該角色有權限的片段（如占卜師僅給其 `seerResult`），或使用 `--public` 參數取得已過濾公開視圖。
- 範例：`node gm-helper.mjs state --public`（若引擎支援）→ 可安全寫入 `game-public.md`；否則 GM 手動摘取公開欄位重組。

## 關鍵原則

1. GM 中立：不預測、不協調、不引導玩家行動
2. 全盲視角：對使用者只呈現公開資訊，絕不洩漏夜間行動/角色身分
3. 綠區輸出紀律：主 session 對使用者的輸出應僅包含遊戲資訊（角色分配結果、行動結果、公開狀態、對話紀錄等）。不得顯示 GM 的思考過程、todo 更新、子代理派發/驗收狀態、或其他程序進程資訊。使用者只需看到「遊戲進行到哪、誰做了什麼、結果如何」。
4. 全員重啟會議機制（無輪次／自然收斂）：建白板（`wolf-meeting.md` / `mason-meeting.md` / `day-meeting.md`）→ 全員同步開始各自獨立思考 → 想好即寫入白板發言紀錄 → 任何人更新後其他所有人（含已發言的）中斷重讀重想再寫 → 全員共識／自然收斂（白天：多數不再想發言 + 投票焦點形成）才停；首句隨機拋球保留；每句後原子落盤 `game-public.md`，下一發言必基於最新白板判斷
5. Session 獨立：每個 session 只讀自己的檔案，不讀其他玩家的
6. 記憶交給 Session：memory.md 由各 session 自己編輯，GM 不碰（除初始化批次寫入，且該批次必須在紅區子 session 原子完成，見 §0）
7. GM 只維護 game-public.md：共享公開狀態由 GM 維護，一句一寫、原子更新（僅 L0 欄位）
8. 終端隔離：紅區執行敏感指令，綠區僅處理公開資訊（見 §0 與資訊控管，L1/L2 禁止直做）
9. 被動能力私下交付：共有者私聊與靈能拂曉結果皆走私聊通道，不經公開紀錄
10. 強制委派：命中 §0.1 L1/L2 即委派 `@fixer` 子 session，主 session 直做視為污染作廢（見 §0.5）
11. 會議編排三鐵則（適用所有全員重啟會議）：
    a) **一次派發**：所有參與者必須在同一時間同時派發，禁止分批、禁止輪次制
    b) **自主循環**：每個參與者在自己 session 內完成全部讀→寫→重讀→重想循環，GM 不中途介入
    c) **只驗收不干預**：GM 只在全部回傳後檢查白板是否達成共識，不在過程中讀白板或轉述

## gm-helper 指令

```bash
# 初始化（紅區執行，寫權威映射）
node gm-helper.mjs init <人數>

# 查看狀態（含私密欄位 ⚠️ 不得直貼角色 session，須過濾或加 --public）
node gm-helper.mjs state
node gm-helper.mjs state --public   # 僅公開視圖，可安全寫入 game-public.md

# 開始白天
node gm-helper.mjs start-day

# 夜間行動（含私密欄位 ⚠️ 返回含占卜/靈能/襲擊明細，須過濾分發）
node gm-helper.mjs night '{seerCheck, wolfKill, guardProtect}'

# 記錄發言（公開，每句後原子寫入 game-public.md）
node gm-helper.mjs speak <id> '<訊息>'

# 投票
node gm-helper.mjs vote '[{voterId, targetId}, ...]'

# 共有者私聊（僅兩共有者可見，不入公開紀錄）
node gm-helper.mjs mason-chat <id> '<訊息>'
```

> ⚠️ 標註「含私密欄位」的指令，其原始輸出禁止未過濾進入角色 session 或綠區。

## 角色配置（15 人局）

| 角色 | 數量 | 說明 |
|------|------|------|
| 村民 | 6 | 無特殊能力 |
| 占卜師 | 1 | 每晚查驗一人，得知「村民」或「非村民」；查村民為重大資訊（Day1 驗村可與共有 2 合計 4 確定村，詳見 Phase 1 註記） |
| 靈能者 | 1 | 每天早上得知前一晚死亡者的角色（拂曉私下交付） |
| 守衛 | 1 | 每晚守護一人，不可連續守同一人，不可自保，Day1 夜不可行動 |
| 共有者 | 2 | 互相知道身分，夜間可私聊；與占卜驗村合計為 Day1 4 確定村來源 |
| 人狼 | 3 | 每晚協調襲擊一人，白天偽裝村民 |
| 狂人 | 1 | 被票出時人狼獲勝，不知道狼是誰；**人狼知道誰是狂人**（己方陣營），不可襲擊狂人 |

## 勝利條件

- 村民陣營：淘汰所有 3 匹人狼
- 人狼陣營：人狼人數 ≥ 村民人數（含狂人）

---

## 附錄

### 附錄 A：Phase 0 Checklist

```markdown
□ 1. 執行 init <N> 後，確認 game-state.json 已生成且人數=N
□ 2. 一次性讀取 game-state.json 全量映射（15 筆）
□ 3. 批次寫入 15 份 memory.md（無遺漏、無重複 persona）
□ 4. 逐 persona 校驗：
     □ P# 一致（memory.md P# == game-state.json P#）
     □ role 一致（memory.md role == game-state.json role）
     □ 同伴一致（wolf/mason 列表與權威映射一致，其餘為空）
□ 5. 校驗失敗 → 全量重寫並重校；通過才進入 game-public.md 建立
□ 6. game-public.md 欄位白名單檢查（見附錄 D）
□ 7. agents.md 標題檢查：無硬編碼 P#，為通用標題
□ 8. Spawn sessions 前確認所有 memory.md 時間戳晚於 game-state.json
```

### 附錄 B：錯誤恢復

| 錯誤情境 | 處理 |
|----------|------|
| `memory.md` 與 `game-state.json` 不一致 | 以 `game-state.json` 為準，批次重寫該 persona 的 `memory.md`，重跑 Checklist |
| `init` 後未執行校驗即開始遊戲 | 立即暫停，補執行步驟 4.5 校驗；已產生的夜間/發言結果作廢 |
| `game-public.md` 洩露私密資訊（角色/夜間行動） | 立即回滾該版本，過濾後重寫；通知所有角色重讀 |
| 守衛 Day1 被詢問並提交 guardProtect | 丟棄該 guardProtect，night 參數改為 null，紀錄註記「Day1 禁守」 |
| `night`/`state` 快照未過濾貼入角色 session | 視為資訊外洩事故，該局作廢或重開；加強紅綠隔離 |
| 會議白板未達自然收斂即進投票 | 自然收斂前不進投票；若投票焦點未形成或多數仍想發言則繼續白板寫→看→重想→再寫循環 |
| 共有者私聊誤寫入 `game-public.md` | 撤回該行，僅保留於兩共有者私聊通道 |
| 靈能結果誤公開而非私下交付 | 撤回公開紀錄，改為私下補發；提醒 GM 遵守拂曉交付時序 |
| `模板/集中生成/單 lane 批量人設` | `整段 discussionLog 作廢，清空 game-state.json discussionLog 與 game-public.md 對話段，按 §2.5 以 15 lane 自由生成重跑，含 heuristic 單進程多句生成（fix-11~18）` |

### 附錄 C：引擎能力同步表

| 引擎能力（engine.ts） | GM_GUIDE 對應 | 說明 |
|-----------------------|---------------|------|
| `init` 隨機分配 + 寫 `game-state.json` | Phase 0 步驟 2 | 權威映射來源 |
| 白板收斂判斷（最近 5 句投票焦點 + 是否還想發言） | Phase 2 自由寫入 + 全員重啟 | 不選人、不設想講分；是否進投票由白板自然收斂決定 |
| 會議終止條件（自然收斂） | Phase 2 終止條件 | 自然收斂（多數不再想發言 + 投票焦點形成），已刪除冷場強停與 25 句上限，無強制下限 |
| `night`（seerCheck/wolfKill/guardProtect → seerResult/mediumInfo/victim） | Phase 1 步驟 4-5 | 含私密欄位，須過濾 |
| `vote` 票型統計與出局判定 | Phase 3 | 公開票數與出局角色 |
| `speak` 寫入對話紀錄 | Phase 2 原子更新 | 一句一寫、一寫一讀；白板新增公開發言經 speak 原子落盤到 game-public.md，見新即重啟 |
| `mason-chat` 私聊底層原語 / `mason-meeting.md` 白板 | Phase 1 共有者會議文件 | 僅兩共有者可見；夜間協調走白板全員重啟，不入公開紀錄 |
| `mediumInfo` 拂曉交付 | Phase 2 前置 | 進入 Day 先私下給靈能者 |
| `guard` 連守限制 + 不可自保 + Day1 禁守 | Phase 1 Day1 判斷 | 連續同對象拒絕，不可守護自己，Day1 強制 null |
| `state --public` 過濾視圖 | 資訊控管快照過濾 | 僅公開欄位可入 green 區 |

> 若引擎新增能力，須同步更新本表與對應 Phase。

### 附錄 D：game-public.md 欄位白名單

**可含（公開資訊）**：
- 遊戲人數、配役配置（各角色數量，不含「誰是誰」）
- 存活名單（P# + 名字 + 存活/死亡狀態）
- 對話紀錄（白天公開發言，含發言者 P# 與內容）
- 投票紀錄（投票明細、票數統計、被票出者及其角色揭露）
- 夜間公開結果（犧牲者 P#，不含襲擊者/守護對象/占卜對象）
- 當前 Day/Night 階段標記

**不可含（私密資訊，禁入 game-public.md）**：
- 任何未公開的角色分配（誰是狼/占卜/靈能/守衛/共有/狂人）
- 夜間行動明細（wolfKill 目標、guardProtect 對象、seerCheck 對象）
- 占卜結果、靈能結果（僅私下交付）
- 共有者私聊內容
- `game-state.json` 原始快照或未過濾 `state`/`night` 輸出

**寫入紀律**：GM 每句發言後原子性更新，僅寫白名單欄位；任何私密欄位寫入視為事故，按附錄 B 回滾。

### 附錄 E：Orchestrator 委派契約與審計（新增，防再犯）

**契約**：
- Orchestrator 為 workflow 管理者，非執行者。命中 §0.1 L1/L2 的工作，**必須** `task(subagent_type="fixer", background=true)` 委派，不得以「更快」「只有一行」為由直做。
- 每個委派須命名驗收人與作用域（如 `fixer:init-batch` 擁有 `game-state.json→memory.md` 原子寫權），主 session 不得與子 session 寫作用域重疊。

**審計**：
- 子 session 回傳必須含：`task_id` + `執行的 gm-helper 指令清單` + `Checklist/白名單檢查結果` + `state --public 過濾視圖`。缺一即視為未完成。
- 主 session 僅憑上述四件轉述公開狀態；不得補讀 `game-state.json` 來「再確認」。

**污染處理**：
- 主 session 若已直讀 L1/L2（如本次 `Read game-state.json`），視為已污染，需在回覆中主動聲明污染並建議重開，或 `task_revive` 由乾淨子 session 重做隔離批次。不得隱瞞後繼續推進遊戲。

### 附錄 F：Day1 討論校驗 Checklist（Phase 2 完成必跑）

- □ 首句僅基於 P6 死訊，無預設他人指控（含首句隨機拋球已執行）
- □ 每句引用或回應上一句或更早白板公開推理，無孤立自說自話；每句必須回應上一句或前 3 句具體內容，禁止無端指控新目標
- □ 無重複句型 >2 次，無固定模板
- □ 白板寫入紀錄顯示自由寫入 + 全員重啟（附每條發言 P# 與所見版次；見新不重啟、單 session 批量代寫多 P 即不通過；無想講分、無選人、無輪次）
- □ 無身分標記（人狼/村民）、無真假暗示、無未公開夜間明細
- □ game-state.json discussionLog 與 game-public.md 對話段句數一致（自然收斂，無上限，超過 35 句不自動疲勞）；收斂判斷僅看白板最近 5 句
- □ 三會議文件（wolf/mason/day-meeting.md）視為 L1 私密衍生，GM 主 session 未直讀（僅經紅區子 session 管理）
任一未通過 → 按附錄 B 重跑
